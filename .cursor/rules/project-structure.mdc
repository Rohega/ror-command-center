---
description: RoR Command Center project structure
alwaysApply: true
---

# Project Structure

**Single source of truth:** `.ai/`

| Directory | Purpose |
|-----------|---------|
| `.ai/agents/` | Rails specialist role definitions (YAML) |
| `.ai/skills/` | Reusable capabilities |
| `.ai/workflows/` | End-to-end processes |
| `.ai/standards/` | Engineering rules (Rails, AWS, PostgreSQL/MySQL, …) |
| `.ai/templates/` | Document templates |

**Specialization:** Ruby on Rails 7+/8+, PostgreSQL & MySQL, Sidekiq/ActiveJob,
Devise, ActiveAdmin, Hotwire, React + Inertia, AWS, Docker, Capistrano, Kamal.

**Adapters (do not duplicate content):**
- `.cursor/rules/` → references `.ai/standards/`
- `.claude/` → Claude Code integration

**Collaboration:** `.ai/standards/collaboration.md` — Question → Options → Decision → Draft → Approval

When implementing features, follow `.ai/workflows/new-feature.yaml`.
