---
description: Rails development standards for RoR Command Center projects
globs: app/**/*.rb,config/**/*.rb,lib/**/*.rb,spec/**/*.rb
alwaysApply: false
---

# Rails Standards

Follow the canonical standard: **`.ai/standards/development.md`**

Key rules:
- Rails conventions over custom abstractions
- Thin controllers; business logic in models/services
- Strong parameters and authorization on every mutating action
- Background jobs for async work

Agent: `.ai/agents/backend-rails-developer.yaml`  
Skills: `review-rails-models`, `create-api-endpoints`, `review-db-migrations`
